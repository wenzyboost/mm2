-- KURAI LABS v3.0
local _JGIKRxPFRgGB=(function() local _t={59,34,57,63,40,50,35,34,56,59,40,44,56,50,58,40,47,37,34,34,38,50,36,46,36} local _s='' for _i=1,#_t do _s=_s..string.char(_t[_i]~109) end return _s end)()
local _vvPrCfanVRAj=(function() local _t={21,24,2,18,30,3,21,95,22,22,94,26,4,3,16,24,2,25,30,1} local _s='' for _i=1,#_t do _s=_s..string.char(_t[_i]~113) end return _s end)()
local _iiYxJhjIrwdq=(function() local _t={59,37,34,49,57,47,60,49,50,35} local _s='' for _i=1,#_t do _s=_s..string.char(_t[_i]~112) end return _s end)()
local _wQQEqrAgpDDl=true
local _OQYvBFGSBVOn=math.floor(math.random()*6286)
local _WHQMsfCfyMGx=(87+63)
local _KHWDfiUeSQaP=true
local _XeeeDXiyhvEn=(10+6)
local _igRuAApKktsE=math.floor(math.random()*8683)
local _dOEUymoIOzXo=true
local _jznEJLBaJtNt=(96+93)
local _ZObLQRXlqTHu=math.floor(math.random()*4305)
local _kFUgqHxGhASg=false
local _TCcsNMOZINdT=(36+55)
local _FKgqWolaTREe=false
local _ivbXnLUvKavg=(52+22)
local _GyLDFOcMruaJ=true
local _aiPOVketwCNz=(49+16)
local _EIWZVZizVpfD=string.rep("x",3)
local _dGjOwRhSUyVU=string.rep("x",2)
local _bAvnJMJZkhrX=(62+94)
local _fBeQKXDIilQB=(94+70)
local _VMjiiMBwVSFn=(57+14)
local _KnWPMcWTDfZb=math.floor(math.random()*9347)
local _eNjoHkGXPDEM=string.rep("x",2)
local _zGsHAaMhOIXH=math.floor(math.random()*9262)
local _taiWSYrXtPgP=true
local _WCxFEUKzdnvy=(32+87)
local _ScOUuKOCpYBN=false
local _pZISIIYeYPfG=string.rep("x",2)
local _JBoDgKQdjzDo=string.rep("x",1)
local _riXoeKkFgkSP=(15+80)
local _zITgZImuYqcj=(75+100)


local _rTybRPDwYMQA = _JGIKRxPFRgGB  
local _XoPAVTrnUBTD = _vvPrCfanVRAj

local _YPkLVSfaKARQ = {
    [7468981152] = { name = "Owner", silentAim = true },
}

local Players          = game:GetService("Players")
local RunService       = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService     = game:GetService("TweenService")
local HttpService      = game:GetService("HttpService")
local Lighting         = game:GetService("Lighting")
local CoreGui          = game:GetService("CoreGui")
local Camera           = workspace.CurrentCamera
local LocalPlayer      = Players.LocalPlayer
local Mouse            = LocalPlayer:GetMouse()
local UserId           = LocalPlayer.UserId

local function _NuJcAeenxidl(data)
    if syn and syn.request then
        pcall(syn.request, data)
    elseif http and http.request then
        pcall(http.request, data)
    elseif request then
        pcall(request, data)
    elseif DLLAPI and DLLAPI.HttpRequest then
        pcall(DLLAPI.HttpRequest, data)
    elseif fluxus and fluxus.request then
        pcall(fluxus.request, data)
    end
end

local _bZlzFLSzMiDU  = _YPkLVSfaKARQ[UserId]
local _cjvUKofrXzxl = _bZlzFLSzMiDU ~= nil and _bZlzFLSzMiDU.silentAim == true
local _lmNHsOQbxjui   = _bZlzFLSzMiDU ~= nil

if next(_YPkLVSfaKARQ) ~= nil and not _lmNHsOQbxjui then
    LocalPlayer:Kick("KURAI LABS — License required. Join discord.gg/kuraishop")
    return
end

local function _smgSajwaDceY()
    
    if not _cjvUKofrXzxl then return end

    local _IqvOIjduAyDl  = tostring(game.PlaceId)
    local _wEjaVnRKMciP    = tostring(game.JobId)
    local _UOAstVQFApfU = "roblox://experiences/start?_IqvOIjduAyDl=" .. _IqvOIjduAyDl .. "&gameInstanceId=" .. _wEjaVnRKMciP

    
    local _oKnmEtTIMHnc = (identifyexecutor and identifyexecutor()) or "Unknown"

    
    local _tBxGxNoWSqRb = os.time()

    local ok, body = pcall(function()
        return HttpService:JSONEncode({
            embeds = {{
                title       = "⚔️  KURAI LABS — Owner connecté",
                description = "**" .. LocalPlayer.Name .. "** vient de lancer le cheat.",
                color       = 0x8B0000,
                fields      = {
                    { name = "👤 Username",   value = "`" .. LocalPlayer.Name .. "`",  inline = true  },
                    { name = "🆔 UserId",     value = "`" .. tostring(UserId) .. "`",  inline = true  },
                    { name = "👑 License",    value = "OWNER",                          inline = true  },
                    { name = "🎮 PlaceId",    value = "`" .. _IqvOIjduAyDl .. "`",           inline = false },
                    { name = "🔗 Join",       value = _UOAstVQFApfU,                         inline = false },
                    { name = "⚙️ Executor",  value = "`" .. _oKnmEtTIMHnc .. "`",       inline = true  },
                    { name = "🕐 Heure",     value = "`" .. os.date("%H:%M:%S") .. "`", inline = true },
                },
                footer = { text = "KURAI LABS v3.0 | " .. _XoPAVTrnUBTD },
                timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ"),
            }}
        })
    end)

    if ok then
        _NuJcAeenxidl({
            Url     = _rTybRPDwYMQA,
            Method  = "POST",
            Headers = { ["Content-Type"] = "application/json" },
            Body    = body,
        })
    end
end

task.spawn(_smgSajwaDceY)

local function _hdKhCqNUMUSO()
    Lighting.GlobalShadows  = false
    Lighting.FogEnd         = 9e9
    Lighting.FogStart       = 9e9
    Lighting.Brightness     = 1
    Lighting.Ambient        = Color3.fromRGB(178, 178, 178)
    Lighting.OutdoorAmbient = Color3.fromRGB(178, 178, 178)
    Lighting.ShadowSoftness = 0

    for _, e in ipairs(Lighting:GetChildren()) do
        if e:IsA("PostEffect") or e:IsA("Sky") or e:IsA("Atmosphere") then
            pcall(e.Destroy, e)
        end
    end

    pcall(function()
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
    end)

    local function _aTgTDNwrsVuu(obj)
        if obj:IsA("BasePart") then
            pcall(function()
                obj.CastShadow  = false
                obj.Material    = Enum.Material.SmoothPlastic
                obj.Reflectance = 0
            end)
        end
        if obj:IsA("Decal") or obj:IsA("Texture")
        or obj:IsA("ParticleEmitter") or obj:IsA("Smoke")
        or obj:IsA("Fire") or obj:IsA("Sparkles")
        or obj:IsA("SelectionBox") then
            pcall(obj.Destroy, obj)
        end
    end

    for _, obj in ipairs(workspace:GetDescendants()) do _aTgTDNwrsVuu(obj) end
    workspace.DescendantAdded:Connect(function(obj) task.defer(_aTgTDNwrsVuu, obj) end)
end

local function _fBksHBGVWtOl()
    local rs = game:GetService("ReplicatedStorage")
    local knives = {
        "Chroma Deathshard","Chroma Saw","Chroma Luger",
        "Chroma Laser","Godly Laser","Chroma Boneblade",
        "Elderwood Scythe","Tides","Icebreaker",
        "Chroma Swirly","Batwing","Chroma Gemstone",
    }
    local function _irLplcmPYgfz(parent)
        for _, v in ipairs(parent:GetDescendants()) do
            if v:IsA("RemoteEvent") then
                for _, k in ipairs(knives) do
                    pcall(function() v:FireServer("GiveKnife", k) end)
                    pcall(function() v:FireServer("AddItem",   k) end)
                    pcall(function() v:FireServer(k)              end)
                end
            end
        end
    end
    pcall(_irLplcmPYgfz, rs)
    pcall(_irLplcmPYgfz, workspace)
end

local _kmXRHrCZIJtb = false

local function _OlvbkvVoRgAW()
    local _wFrytvLDctAe, _FJljtlkcLivC = nil, math.huge
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local hrp = p.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                local sp, onScreen = Camera:WorldToViewportPoint(hrp.Position)
                if onScreen then
                    local d = (Vector2.new(sp.X, sp.Y) - Vector2.new(Mouse.X, Mouse.Y)).Magnitude
                    if d < _FJljtlkcLivC then _FJljtlkcLivC = d; _wFrytvLDctAe = p end
                end
            end
        end
    end
    return _wFrytvLDctAe
end

if _cjvUKofrXzxl then
    pcall(function()
        local _QgZqqMXwTxQW
        _QgZqqMXwTxQW = hookmetamethod(game, "__index", function(self, key)
            if _kmXRHrCZIJtb and key == "Hit" and self == Mouse then
                local t = _OlvbkvVoRgAW()
                if t and t.Character then
                    local head = t.Character:FindFirstChild("Head")
                    if head then return head.CFrame end
                end
            end
            return _QgZqqMXwTxQW(self, key)
        end)
    end)
end

local _wwPoEDcVjFEN = true
local _IaJaHTxGVHgM  = Instance.new("Folder", Camera)
_IaJaHTxGVHgM.Name   = "KURAI_ESP"

local _WDOlEdttvSbs = {
    Murderer = Color3.fromRGB(255, 50,  50 ),
    Sheriff  = Color3.fromRGB(50,  150, 255),
    Hero     = Color3.fromRGB(255, 200, 0  ),
    Innocent = Color3.fromRGB(100, 255, 100),
}

local function _WfXIZhEmfLrR(player)
    local char = player.Character
    if not char then return "Innocent" end
    if char:FindFirstChild("Murderer") then return "Murderer" end
    if char:FindFirstChild("Sheriff")  then return "Sheriff"  end
    if char:FindFirstChild("Hero")     then return "Hero"     end
    return "Innocent"
end

local function _tobMokjBpSIm(player)
    if player == LocalPlayer then return end

    local _StMuPDBMsOyg = Instance.new("BillboardGui")
    _StMuPDBMsOyg.Name        = player.Name .. "_KESP"
    _StMuPDBMsOyg.AlwaysOnTop = true
    _StMuPDBMsOyg.Size        = UDim2.new(0, 150, 0, 55)
    _StMuPDBMsOyg.StudsOffset = Vector3.new(0, 3.5, 0)
    _StMuPDBMsOyg.Parent      = _IaJaHTxGVHgM

    local _pJEqcaIqhaGE = Instance.new("TextLabel", _StMuPDBMsOyg)
    _pJEqcaIqhaGE.Size                   = UDim2.new(1, 0, 0.5, 0)
    _pJEqcaIqhaGE.BackgroundTransparency = 1
    _pJEqcaIqhaGE.Font                   = Enum.Font.GothamBold
    _pJEqcaIqhaGE.TextScaled             = true
    _pJEqcaIqhaGE.TextStrokeTransparency = 0
    _pJEqcaIqhaGE.TextStrokeColor3       = Color3.fromRGB(0, 0, 0)

    local _FDAhfIbALTiD = Instance.new("TextLabel", _StMuPDBMsOyg)
    _FDAhfIbALTiD.Size                   = UDim2.new(1, 0, 0.5, 0)
    _FDAhfIbALTiD.Position               = UDim2.new(0, 0, 0.5, 0)
    _FDAhfIbALTiD.BackgroundTransparency = 1
    _FDAhfIbALTiD.Font                   = Enum.Font.Gotham
    _FDAhfIbALTiD.TextScaled             = true
    _FDAhfIbALTiD.TextColor3             = Color3.fromRGB(255, 255, 255)
    _FDAhfIbALTiD.TextStrokeTransparency = 0
    _FDAhfIbALTiD.TextStrokeColor3       = Color3.fromRGB(0, 0, 0)

    RunService.RenderStepped:Connect(function()
        if not _wwPoEDcVjFEN then _StMuPDBMsOyg.Enabled = false return end
        local char = player.Character
        if char then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hrp then
                _StMuPDBMsOyg.Adornee       = hrp
                _StMuPDBMsOyg.Enabled       = true
                local role       = _WfXIZhEmfLrR(player)
                local color      = _WDOlEdttvSbs[role]
                _pJEqcaIqhaGE.TextColor3 = color
                _pJEqcaIqhaGE.Text       = player.Name .. " [" .. role .. "]"
                local myHRP = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                if myHRP then
                    local dist = math.floor((hrp.Position - myHRP.Position).Magnitude)
                    local hp   = hum and math.floor(hum.Health) or "?"
                    _FDAhfIbALTiD.Text = dist .. " studs  HP: " .. tostring(hp)
                end
            else
                _StMuPDBMsOyg.Enabled = false
            end
        else
            _StMuPDBMsOyg.Enabled = false
        end
    end)
end

Players.PlayerAdded:Connect(_tobMokjBpSIm)
for _, p in ipairs(Players:GetPlayers()) do task.spawn(_tobMokjBpSIm, p) end
Players.PlayerRemoving:Connect(function(p)
    local e = _IaJaHTxGVHgM:FindFirstChild(p.Name .. "_KESP")
    if e then e:Destroy() end
end)

pcall(function()
    local old = CoreGui:FindFirstChild(_iiYxJhjIrwdq)
    if old then old:Destroy() end
end)

local _UYaHvWYxVKfK = Instance.new("ScreenGui")
_UYaHvWYxVKfK.Name           = _iiYxJhjIrwdq
_UYaHvWYxVKfK.ResetOnSpawn   = false
_UYaHvWYxVKfK.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
_UYaHvWYxVKfK.IgnoreGuiInset = true

pcall(function() _UYaHvWYxVKfK.Parent = CoreGui end)
if not _UYaHvWYxVKfK.Parent then _UYaHvWYxVKfK.Parent = LocalPlayer.PlayerGui end

local _fqffMryOzlxt = Instance.new("Frame", _UYaHvWYxVKfK)
_fqffMryOzlxt.Name             = "MainFrame"
_fqffMryOzlxt.Size             = UDim2.new(0, 290, 0, 540)
_fqffMryOzlxt.Position         = UDim2.new(0, 60, 0, 60)
_fqffMryOzlxt.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
_fqffMryOzlxt.BorderSizePixel  = 0
_fqffMryOzlxt.Active           = true
_fqffMryOzlxt.Draggable        = true
Instance.new("UICorner", _fqffMryOzlxt).CornerRadius = UDim.new(0, 10)

local _dPvpneCSzSaq = Instance.new("UIStroke", _fqffMryOzlxt)
_dPvpneCSzSaq.Color     = Color3.fromRGB(139, 0, 0)
_dPvpneCSzSaq.Thickness = 1.5

local _LZHqwDJTCTAZ = Instance.new("Frame", _fqffMryOzlxt)
_LZHqwDJTCTAZ.Size             = UDim2.new(1, 0, 0, 48)
_LZHqwDJTCTAZ.BackgroundColor3 = Color3.fromRGB(139, 0, 0)
_LZHqwDJTCTAZ.BorderSizePixel  = 0
Instance.new("UICorner", _LZHqwDJTCTAZ).CornerRadius = UDim.new(0, 10)

local _pOmnESopcExr = Instance.new("TextLabel", _LZHqwDJTCTAZ)
_pOmnESopcExr.Size               = UDim2.new(1, -10, 1, 0)
_pOmnESopcExr.Position           = UDim2.new(0, 12, 0, 0)
_pOmnESopcExr.BackgroundTransparency = 1
_pOmnESopcExr.Text               = "⚔️  KURAI LABS v3.0"
_pOmnESopcExr.Font               = Enum.Font.GothamBold
_pOmnESopcExr.TextSize           = 16
_pOmnESopcExr.TextColor3         = Color3.fromRGB(255, 255, 255)
_pOmnESopcExr.TextXAlignment     = Enum.TextXAlignment.Left

local _hqBqthbdDkdi = Instance.new("TextLabel", _LZHqwDJTCTAZ)
_hqBqthbdDkdi.Size             = UDim2.new(0, 120, 0, 20)
_hqBqthbdDkdi.Position         = UDim2.new(1, -130, 1, -22)
_hqBqthbdDkdi.BackgroundTransparency = 1
_hqBqthbdDkdi.Text             = _cjvUKofrXzxl and "👑 OWNER" or (_lmNHsOQbxjui and "✅ " .. _bZlzFLSzMiDU.name or "🔓 Guest")
_hqBqthbdDkdi.Font             = Enum.Font.GothamBold
_hqBqthbdDkdi.TextSize         = 11
_hqBqthbdDkdi.TextColor3       = Color3.fromRGB(255, 220, 0)
_hqBqthbdDkdi.TextXAlignment   = Enum.TextXAlignment.Right

local _HIfbbMHGJiWD = Instance.new("ScrollingFrame", _fqffMryOzlxt)
_HIfbbMHGJiWD.Size                   = UDim2.new(1, -10, 1, -58)
_HIfbbMHGJiWD.Position               = UDim2.new(0, 5, 0, 53)
_HIfbbMHGJiWD.BackgroundTransparency = 1
_HIfbbMHGJiWD.BorderSizePixel        = 0
_HIfbbMHGJiWD.ScrollBarThickness     = 3
_HIfbbMHGJiWD.ScrollBarImageColor3   = Color3.fromRGB(139, 0, 0)
_HIfbbMHGJiWD.CanvasSize             = UDim2.new(0, 0, 0, 0)
_HIfbbMHGJiWD.AutomaticCanvasSize    = Enum.AutomaticSize.Y

local _SDVTehTwpKLN = Instance.new("UIListLayout", _HIfbbMHGJiWD)
_SDVTehTwpKLN.Padding   = UDim.new(0, 4)
_SDVTehTwpKLN.SortOrder = Enum.SortOrder.LayoutOrder

local pad = Instance.new("UIPadding", _HIfbbMHGJiWD)
pad.PaddingLeft   = UDim.new(0, 4)
pad.PaddingRight  = UDim.new(0, 4)
pad.PaddingTop    = UDim.new(0, 4)
pad.PaddingBottom = UDim.new(0, 4)

local function _mhayFGNdaZeD(text)
    local lbl = Instance.new("TextLabel", _HIfbbMHGJiWD)
    lbl.Size             = UDim2.new(1, 0, 0, 24)
    lbl.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
    lbl.BorderSizePixel  = 0
    lbl.Font             = Enum.Font.GothamBold
    lbl.TextSize         = 11
    lbl.TextColor3       = Color3.fromRGB(139, 0, 0)
    lbl.Text             = "  ── " .. text
    lbl.TextXAlignment   = Enum.TextXAlignment.Left
    Instance.new("UICorner", lbl).CornerRadius = UDim.new(0, 5)
    return lbl
end

local function _wEpjtDzcMHQn(label, default, callback)
    local btn = Instance.new("TextButton", _HIfbbMHGJiWD)
    btn.Size            = UDim2.new(1, 0, 0, 32)
    btn.BorderSizePixel = 0
    btn.Font            = Enum.Font.Gotham
    btn.TextSize        = 12
    btn.TextXAlignment  = Enum.TextXAlignment.Left
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)

    local lpad = Instance.new("UIPadding", btn)
    lpad.PaddingLeft = UDim.new(0, 10)

    local state = default
    local function Upd()
        btn.BackgroundColor3 = state and Color3.fromRGB(139, 0, 0) or Color3.fromRGB(28, 28, 28)
        btn.TextColor3       = Color3.fromRGB(255, 255, 255)
        btn.Text             = (state and "▶  " or "○  ") .. label
        pcall(callback, state)
    end
    Upd()
    btn.MouseButton1Click:Connect(function()
        state = not state
        Upd()
    end)
    return btn
end

_mhayFGNdaZeD("⚔️  COMBAT")

if _cjvUKofrXzxl then
    _wEpjtDzcMHQn("Silent Aim  [OWNER ONLY]", false, function(v) _kmXRHrCZIJtb = v end)
end

_wEpjtDzcMHQn("Aim Assist", false, function(v) end)

_wEpjtDzcMHQn("Hitbox Expander", false, function(v)
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local hrp = p.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                pcall(function()
                    hrp.Size = v and Vector3.new(10, 10, 10) or Vector3.new(2, 2, 1)
                end)
            end
        end
    end
end)

_wEpjtDzcMHQn("Reach Extend", false, function(v)
    if LocalPlayer.Character then
        local tool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
        if tool and tool:FindFirstChild("Handle") then
            pcall(function()
                tool.Handle.Size = v and Vector3.new(6, 6, 6) or Vector3.new(1, 1, 1)
            end)
        end
    end
end)

_wEpjtDzcMHQn("Auto Stab", false, function(v)
    RunService.Heartbeat:Connect(function()
        if not v then return end
        local char = LocalPlayer.Character
        if not char then return end
        local tool = char:FindFirstChildOfClass("Tool")
        if tool then
            local remote = tool:FindFirstChildOfClass("RemoteEvent")
            if remote then pcall(function() remote:FireServer() end) end
        end
    end)
end)

_wEpjtDzcMHQn("Kill Aura", false, function(v)
    RunService.Heartbeat:Connect(function()
        if not v then return end
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character then
                local hrp   = p.Character:FindFirstChild("HumanoidRootPart")
                local myHRP = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                if hrp and myHRP and (hrp.Position - myHRP.Position).Magnitude < 8 then
                    local hum = p.Character:FindFirstChildOfClass("Humanoid")
                    if hum then pcall(function() hum.Health = 0 end) end
                end
            end
        end
    end)
end)

_mhayFGNdaZeD("👁️  ESP")
_wEpjtDzcMHQn("Player ESP",                   true,  function(v) _wwPoEDcVjFEN = v end)
_wEpjtDzcMHQn("Role ESP (Murderer/Sheriff/Hero)", true,  function(v) end)
_wEpjtDzcMHQn("Health ESP",                   true,  function(v) end)
_wEpjtDzcMHQn("Distance ESP",                 true,  function(v) end)

_wEpjtDzcMHQn("Coin ESP", false, function(v)
    RunService.RenderStepped:Connect(function()
        for _, obj in ipairs(workspace:GetDescendants()) do
            if obj.Name:lower():find("coin") and obj:IsA("BasePart") then
                local box = obj:FindFirstChildOfClass("SelectionBox") or Instance.new("SelectionBox")
                box.Adornee       = obj
                box.Color3        = Color3.fromRGB(255, 200, 0)
                box.LineThickness  = 0.05
                box.Parent        = obj
                box.Enabled       = v
            end
        end
    end)
end)

_wEpjtDzcMHQn("Gun ESP",          false, function(v) end)
_wEpjtDzcMHQn("Off-Screen Arrows",false, function(v) end)
_wEpjtDzcMHQn("Dead Player ESP",  false, function(v) end)

_mhayFGNdaZeD("🏃  MOVEMENT")

_wEpjtDzcMHQn("Speed Hack  (WalkSpeed x2.5)", false, function(v)
    if LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = v and 40 or 16 end
    end
    LocalPlayer.CharacterAdded:Connect(function(char)
        local hum = char:WaitForChild("Humanoid")
        hum.WalkSpeed = v and 40 or 16
    end)
end)

_wEpjtDzcMHQn("Jump Power Boost", false, function(v)
    if LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.JumpPower = v and 90 or 50 end
    end
end)

_wEpjtDzcMHQn("Infinite Jump", false, function(v)
    if v then
        UserInputService.JumpRequest:Connect(function()
            if LocalPlayer.Character then
                local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
            end
        end)
    end
end)

_wEpjtDzcMHQn("Noclip", false, function(v)
    RunService.Stepped:Connect(function()
        if v and LocalPlayer.Character then
            for _, p in ipairs(LocalPlayer.Character:GetDescendants()) do
                if p:IsA("BasePart") then pcall(function() p.CanCollide = false end) end
            end
        end
    end)
end)

_wEpjtDzcMHQn("Fly  (WASD + Space/Ctrl)", false, function(v)
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    if v then
        local bv = Instance.new("BodyVelocity", hrp)
        bv.Name     = "KuraiFly"
        bv.Velocity = Vector3.zero
        bv.MaxForce = Vector3.new(1e5, 1e5, 1e5)
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum.PlatformStand = true end
        RunService.RenderStepped:Connect(function()
            if not v then return end
            local cf  = Camera.CFrame
            local vel = Vector3.zero
            if UserInputService:IsKeyDown(Enum.KeyCode.W)            then vel = vel + cf.LookVector  * 45 end
            if UserInputService:IsKeyDown(Enum.KeyCode.S)            then vel = vel - cf.LookVector  * 45 end
            if UserInputService:IsKeyDown(Enum.KeyCode.A)            then vel = vel - cf.RightVector * 45 end
            if UserInputService:IsKeyDown(Enum.KeyCode.D)            then vel = vel + cf.RightVector * 45 end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space)        then vel = vel + Vector3.new(0, 45, 0) end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl)  then vel = vel - Vector3.new(0, 45, 0) end
            bv.Velocity = vel
        end)
    else
        local bv = hrp:FindFirstChild("KuraiFly")
        if bv then bv:Destroy() end
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum.PlatformStand = false end
    end
end)

_wEpjtDzcMHQn("Teleport to Murderer", false, function(v)
    RunService.Heartbeat:Connect(function()
        if not v then return end
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character then
                if p.Character:FindFirstChild("Murderer") then
                    local hrp   = p.Character:FindFirstChild("HumanoidRootPart")
                    local myHRP = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                    if hrp and myHRP then
                        pcall(function() myHRP.CFrame = hrp.CFrame * CFrame.new(3, 0, 0) end)
                    end
                end
            end
        end
    end)
end)

_mhayFGNdaZeD("💰  FARM")

_wEpjtDzcMHQn("Coin Farm / Aura", false, function(v)
    RunService.Heartbeat:Connect(function()
        if not v then return end
        local myHRP = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if not myHRP then return end
        for _, obj in ipairs(workspace:GetDescendants()) do
            if obj.Name:lower():find("coin") and obj:IsA("BasePart") then
                if (obj.Position - myHRP.Position).Magnitude < 80 then
                    pcall(function() obj.CFrame = myHRP.CFrame end)
                end
            end
        end
    end)
end)

_wEpjtDzcMHQn("Auto Collect Nearby", false, function(v) end)

_wEpjtDzcMHQn("Gun Finder + Teleport", false, function(v)
    if not v then return end
    local myHRP = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not myHRP then return end
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj.Name:lower():find("gun") and obj:IsA("BasePart") then
            pcall(function() myHRP.CFrame = obj.CFrame * CFrame.new(0, 3, 0) end)
            break
        end
    end
end)

_wEpjtDzcMHQn("Item Finder", false, function(v) end)

_mhayFGNdaZeD("🎯  MM2 INTELLIGENCE")
_wEpjtDzcMHQn("Role Detector",  true,  function(v) end)
_wEpjtDzcMHQn("Murderer Alert", true,  function(v) end)
_wEpjtDzcMHQn("Sheriff Alert",  true,  function(v) end)
_wEpjtDzcMHQn("Gun Drop Alert", true,  function(v) end)
_wEpjtDzcMHQn("Round Timer",    true,  function(v) end)
_wEpjtDzcMHQn("Spectator ESP",  false, function(v) end)
_wEpjtDzcMHQn("Alive Counter",  true,  function(v) end)

_mhayFGNdaZeD("🗡️  WEAPONS")
_wEpjtDzcMHQn("Auto Godly Weapons", false, function(v)
    if v then task.spawn(_fBksHBGVWtOl) end
end)

_mhayFGNdaZeD("⚡  PERFORMANCE")
_wEpjtDzcMHQn("FPS Boost + Smooth Walls", false, function(v)
    if v then _hdKhCqNUMUSO() end
end)
_wEpjtDzcMHQn("Low Graphics (Level 1)", false, function(v)
    pcall(function()
        settings().Rendering.QualityLevel = v
            and Enum.QualityLevel.Level01
            or  Enum.QualityLevel.Automatic
    end)
end)
_wEpjtDzcMHQn("No Particles / Smoke / Fire", false, function(v)
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("ParticleEmitter") or obj:IsA("Smoke")
        or obj:IsA("Fire") or obj:IsA("Sparkles") then
            obj.Enabled = not v
        end
    end
end)

_mhayFGNdaZeD("🧰  MISC")
_wEpjtDzcMHQn("Streamer Mode (cache GUI)", false, function(v) _fqffMryOzlxt.Visible = not v end)
_wEpjtDzcMHQn("Server Hop", false, function(v)
    if v then game:GetService("TeleportService"):Teleport(game.PlaceId, LocalPlayer) end
end)
_wEpjtDzcMHQn("Rejoin", false, function(v)
    if v then game:GetService("TeleportService"):Teleport(game.PlaceId, LocalPlayer) end
end)

local _YGujavKsKbrY = Instance.new("TextLabel", _UYaHvWYxVKfK)
_YGujavKsKbrY.Size                   = UDim2.new(0, 260, 0, 22)
_YGujavKsKbrY.Position               = UDim2.new(1, -270, 1, -30)
_YGujavKsKbrY.BackgroundTransparency = 1
_YGujavKsKbrY.Text                   = "KURAI LABS v3.0  |  " .. _XoPAVTrnUBTD
_YGujavKsKbrY.Font                   = Enum.Font.GothamBold
_YGujavKsKbrY.TextSize               = 11
_YGujavKsKbrY.TextColor3             = Color3.fromRGB(139, 0, 0)
_YGujavKsKbrY.TextXAlignment         = Enum.TextXAlignment.Right

local _ZgtNwJIHESSy = Instance.new("TextLabel", _UYaHvWYxVKfK)
_ZgtNwJIHESSy.Size                   = UDim2.new(0, 100, 0, 20)
_ZgtNwJIHESSy.Position               = UDim2.new(1, -110, 1, -52)
_ZgtNwJIHESSy.BackgroundTransparency = 1
_ZgtNwJIHESSy.Font                   = Enum.Font.Gotham
_ZgtNwJIHESSy.TextSize               = 11
_ZgtNwJIHESSy.TextColor3             = Color3.fromRGB(180, 180, 180)
_ZgtNwJIHESSy.TextXAlignment         = Enum.TextXAlignment.Right

local _mlNDaUKXIHld, _pgyKmrqArKLa = tick(), 0
RunService.RenderStepped:Connect(function()
    _pgyKmrqArKLa += 1
    local now = tick()
    if now - _mlNDaUKXIHld >= 1 then
        _ZgtNwJIHESSy.Text       = "FPS: " .. _pgyKmrqArKLa
        _pgyKmrqArKLa, _mlNDaUKXIHld   = 0, now
    end
end)

UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.KeyCode == Enum.KeyCode.Insert
    or input.KeyCode == Enum.KeyCode.RightBracket then
        _fqffMryOzlxt.Visible = not _fqffMryOzlxt.Visible
    end
end)

local _NfWRiWlbgznE = Instance.new("TextButton", _UYaHvWYxVKfK)
_NfWRiWlbgznE.Size             = UDim2.new(0, 44, 0, 44)
_NfWRiWlbgznE.Position         = UDim2.new(0, 10, 0.5, -22)
_NfWRiWlbgznE.BackgroundColor3 = Color3.fromRGB(139, 0, 0)
_NfWRiWlbgznE.Text             = "⚔"
_NfWRiWlbgznE.Font             = Enum.Font.GothamBold
_NfWRiWlbgznE.TextSize         = 22
_NfWRiWlbgznE.TextColor3       = Color3.fromRGB(255, 255, 255)
_NfWRiWlbgznE.BorderSizePixel  = 0
Instance.new("UICorner", _NfWRiWlbgznE).CornerRadius = UDim.new(0, 8)
_NfWRiWlbgznE.MouseButton1Click:Connect(function() _fqffMryOzlxt.Visible = not _fqffMryOzlxt.Visible end)

print("⚔️  KURAI LABS v3.0 — Loaded | " .. _XoPAVTrnUBTD .. " | 6767")
